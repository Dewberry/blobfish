""" Synchronous operations for creating AORC data mirror on s3"""
import requests
import shapely
import datetime


def create_rfc_list(rfc_tar_shp_url: str) -> list:
    return []


def create_potential_urls(rfcs: list, start_dt: datetime.datetime) -> list:
    return []


def check_zip_metadata(zip_fn: str) -> dict:
    return {}
