{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id": "https://ckan-url/script.json#",
    "title": "Transposition Script",
    "description": "Script used to start the model which generated modeled data output.",
    "type": "object",
    "required": [
        "dockerImage"
    ],
    "properties": {
        "@type": {
            "title": "Metadata Context",
            "description": "IRI for the JSON-LD data type. This should be aorc:TranspositionScript",
            "enum": [
                "aorcTranspositionScript"
            ]
        },
        "dockerImage": {
            "title": "Docker Image",
            "description": "Docker image which was used to launch the transposition script",
            "$ref": "https://ckan-url/dockerImage.json"
        }
    }
}